function sumAll(arr) {
  var big;
  var small;
  var sum=0;
  if (arr[1]>arr[0])
  {
    big=arr[1];
    small=arr[0];
  }
  else
  {
    big=arr[0];
    small=arr[1];
  }
  for (let i = small;i<=big;i++)
  {
    sum+=i;
  }
  return sum;
}

console.log(sumAll([1, 4]));
